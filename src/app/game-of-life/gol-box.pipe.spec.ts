import { GolBoxPipe } from './gol-box.pipe';

describe('GolBoxPipe', () => {
    it('create an instance', () => {
        const pipe = new GolBoxPipe();
        expect(pipe).toBeTruthy();
    });
});
